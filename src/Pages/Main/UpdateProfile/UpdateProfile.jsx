import { Link } from "react-router-dom";

const UpdateProfile = () => {
//   const id = useParams();
//   console.log(id);
  return (
    <>
      <Link to={`/update-profile`}></Link>
    </>
  );
};

export default UpdateProfile;
